#include "main.h"

/**
 * add - Adds two integers and returns the result
 *
 * @a: is an integer
 * @b: is an integer
 *
 * Return: Sum of two integers
 */
int add(int a, int b)
{
	return (a + b);
	_putchar(10);
}
