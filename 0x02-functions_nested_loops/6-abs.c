#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *
 * @a: is an integer
 * Return: An absolute value.
 */
int _abs(int a)
{
	int b, c;

	if (a >= 0)
	{
		return (a);
	}
	else
	{
		c = b;
		b *= 2;
		c = b / c;
		return (c);
	}
	_putchar(10);
}
